* node 14.2.0 安装 puppeteer
  * 安装 puppeteer(16.1.0)
  ```shell
    PUPPETEER_DOWNLOAD_HOST=https://cdn.npmmirror.com/binaries \ ### 替换为国内镜像地址,如果超时网上可以搜索新的镜像地址
    npm install puppeteer@16.1.0
  ```
  

    